const convertonode=(parentnode, childnode)=>
    {
        const parent=[];
        const child=[];
        const node =[];

        parent = parentnode;
        child = childnode;
        
        let obj = {};
        parent.forEach(par => {    
            
        });

        return(       
        {node}
        );
    }

